{
  "name": "gdocs-sync",
  "version": "0.0.1",
  "private": true,
  "description": "Standalone markdown <-> Google Docs sync engine: push, fetch, comments, tabs, images. Fresh implementation against the dru89/doc-tools scenario catalog. Consumers: Margin (embedded) and a future gpush/gfetch-compatible CLI. Extracts to its own repo when stable.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "auth": "node src/auth.ts",
    "smoke": "node scripts/smoke.ts",
    "rt1": "node scripts/rt1.ts",
    "test:live": "vitest run --config vitest.live.config.ts"
  },
  "devDependencies": {
    "@types/mdast": "^4.0.4",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "mdast-util-from-markdown": "^2.0.1",
    "mdast-util-gfm": "^3.0.0",
    "micromark-extension-gfm": "^3.0.0"
  }
}